# Решение проблем с автозапуском

Если ничего не работает, выполните эти шаги по порядку:

## 🔍 Шаг 1: Диагностика

Сначала запустите диагностический скрипт, чтобы понять, в чём проблема:

1. Откройте **Проводник Windows**
2. Перейдите в: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл **`diagnose_network.ps1`**
4. **Правой кнопкой мыши** → **Запустить с PowerShell**

Скрипт покажет все проблемы и способы их решения.

---

## 🛠️ Шаг 2: Исправление проблем

### Проблема 1: "Execution policy is Restricted"

**Решение:**
1. Откройте PowerShell как администратор
2. Выполните:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
3. Нажмите `Y` для подтверждения

---

### Проблема 2: "Cannot access network path"

**Решение A: Сопоставить сетевой диск**

1. Откройте **Командную строку как администратор**
2. Выполните:
   ```cmd
   net use Z: "\\Desktop-1\новая папка"
   ```
3. Теперь используйте путь: `Z:\code\debt-track`

**Решение B: Использовать проводник**

1. Откройте **Проводник Windows**
2. В адресной строке введите: `\\Desktop-1\новая папка\code\debt-track`
3. Если папка открывается - всё в порядке
4. В этой папке найдите `setup_autostart_FINAL.ps1`
5. **Правой кнопкой** → **Запустить с PowerShell**

---

### Проблема 3: "Python not found"

**Решение:**
1. Установите Python с python.org
2. При установке отметьте "Add Python to PATH"
3. Или используйте виртуальное окружение в проекте (venv)

---

## ✅ Шаг 3: Финальная настройка

После исправления проблем:

1. Откройте **Проводник Windows**
2. Перейдите в: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл **`setup_autostart_FINAL.ps1`**
4. **Правой кнопкой мыши** → **Запустить с PowerShell**

Этот скрипт:
- ✅ Автоматически проверит все требования
- ✅ Исправит проблемы, если возможно
- ✅ Создаст задачу в Планировщике заданий
- ✅ Покажет подробные инструкции при ошибках

---

## 🎯 Альтернатива: Ручная настройка

Если скрипты всё равно не работают, настройте вручную:

### 1. Сопоставьте сетевой диск

```cmd
net use Z: "\\Desktop-1\новая папка"
```

### 2. Создайте задачу в Планировщике заданий

1. **Win + R** → `taskschd.msc` → Enter
2. **Создать простую задачу** (справа)
3. **Имя:** `PharmacyDebtTracker`
4. **Триггер:** "При входе в систему"
5. **Действие:** "Запустить программу"
6. **Программа:** 
   - Если есть venv: `Z:\code\debt-track\venv\Scripts\pythonw.exe`
   - Или: `pythonw.exe`
7. **Аргументы:** `manage.py runserver 0.0.0.0:8000`
8. **Начать в:** `Z:\code\debt-track`
9. **Свойства** → **Общие**:
   - ✅ "Выполнять с наивысшими правами"
   - Выбрать "Выполнять только при входе пользователя"

### 3. Проверка

1. Откройте Планировщик заданий
2. Найдите задачу `PharmacyDebtTracker`
3. **Правой кнопкой** → **Выполнить**
4. Откройте браузер: `http://127.0.0.1:8000`

---

## 📞 Если ничего не помогает

1. Запустите `diagnose_network.ps1` и пришлите полный вывод
2. Проверьте, что:
   - Компьютер Desktop-1 включен
   - Сетевое подключение работает
   - Вы можете открыть папку в проводнике
   - Python установлен и работает

---

## 💡 Полезные команды

```powershell
# Проверить сетевой путь
Test-Path "\\Desktop-1\новая папка\code\debt-track"

# Сопоставить диск
net use Z: "\\Desktop-1\новая папка"

# Проверить Python
python --version

# Проверить задачу
Get-ScheduledTask -TaskName "PharmacyDebtTracker"
```




