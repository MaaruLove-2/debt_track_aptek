# Быстрая настройка автозапуска

## ⚡ Самый простой способ (рекомендуется):

### Вариант 1: Через проводник Windows

1. Откройте проводник Windows
2. Перейдите в папку проекта: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл `setup_autostart_as_admin.ps1`
4. **Правой кнопкой мыши** → **Запустить с PowerShell** (или "Запуск с правами администратора")

Готово! ✅

---

### Вариант 2: Через PowerShell (как администратор)

1. **Правой кнопкой мыши** на кнопке Пуск → **Windows PowerShell (администратор)**
   - Или найдите PowerShell в меню Пуск → правой кнопкой → **Запуск от имени администратора**

2. Выполните команды:
   ```powershell
   cd "\\Desktop-1\новая папка\code\debt-track"
   .\setup_network_autostart_simple.ps1
   ```

Готово! ✅

---

### Вариант 3: Через batch-файл

1. Откройте проводник Windows
2. Перейдите в папку проекта: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл `setup_autostart_as_admin.bat`
4. **Правой кнопкой мыши** → **Запуск от имени администратора**

Готово! ✅

---

## ⚠️ Важно!

**НЕ используйте обычную командную строку (CMD)** - она не поддерживает UNC-пути напрямую.

Используйте:
- ✅ PowerShell (рекомендуется)
- ✅ Проводник Windows (правой кнопкой → Запуск с PowerShell)
- ✅ Batch-файл (который запустит PowerShell)

---

## 🔍 Проверка работы

После настройки:

1. Откройте **Планировщик заданий** (`Win + R` → `taskschd.msc`)
2. Найдите задачу **"PharmacyDebtTracker"**
3. **Правой кнопкой** → **Выполнить**
4. Откройте браузер: `http://127.0.0.1:8000`

Если страница открывается - всё работает! ✅

---

## 🛑 Если что-то не работает

### Проблема: "Не удается получить доступ к сетевому пути"

**Решение:**
1. Убедитесь, что компьютер Desktop-1 включен
2. Проверьте, что вы можете открыть папку в проводнике Windows
3. Попробуйте сопоставить сетевой диск:
   - Откройте "Этот компьютер"
   - Вкладка "Компьютер" → "Подключить сетевой диск"
   - Выберите букву (например, Z:)
   - Путь: `\\Desktop-1\новая папка`
   - Затем используйте: `Z:\code\debt-track`

### Проблема: "Требуются права администратора"

**Решение:**
- Всегда запускайте скрипты **правой кнопкой мыши** → **Запуск от имени администратора**

### Проблема: "Python не найден"

**Решение:**
- Установите Python или добавьте его в PATH
- Или используйте виртуальное окружение (venv) в папке проекта

---

## 📝 Ручная настройка (если скрипты не работают)

1. **Win + R** → `taskschd.msc` → Enter
2. **Создать простую задачу** (справа)
3. **Имя:** "PharmacyDebtTracker"
4. **Триггер:** "При входе в систему"
5. **Действие:** "Запустить программу"
6. **Программа:** 
   - Если есть venv: `\\Desktop-1\новая папка\code\debt-track\venv\Scripts\pythonw.exe`
   - Или: `pythonw.exe` (если в PATH)
7. **Аргументы:** `manage.py runserver 0.0.0.0:8000`
8. **Начать в:** `\\Desktop-1\новая папка\code\debt-track`
9. В **Свойствах** → **Общие**:
   - ✅ Отметить "Выполнять с наивысшими правами"
   - Выбрать "Выполнять только при входе пользователя в систему"

Готово! ✅





