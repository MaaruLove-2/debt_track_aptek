# Создание ярлыка на рабочем столе

## 🎯 Автоматическое создание (рекомендуется)

1. Откройте **Проводник Windows**
2. Перейдите в: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл **`create_desktop_shortcut_silent.ps1`**
4. **Правой кнопкой мыши** → **Запустить с PowerShell**

Готово! На рабочем столе появится ярлык "Pharmacy Server" ✅

---

## 🔧 Ручное создание ярлыка

Если автоматический скрипт не работает:

### Шаг 1: Создайте ярлык

1. **Правой кнопкой мыши** на рабочем столе → **Создать** → **Ярлык**

2. В поле "Расположение объекта" введите:
   ```
   wscript.exe "\\Desktop-1\новая папка\code\debt-track\launch_server_silent.vbs"
   ```

3. Нажмите **Далее**

4. Назовите ярлык: **Pharmacy Server**

5. Нажмите **Готово**

---

### Шаг 2: Настройте ярлык (опционально)

1. **Правой кнопкой мыши** на ярлыке → **Свойства**

2. Вкладка **Ярлык**:
   - **Рабочая папка**: `\\Desktop-1\новая папка\code\debt-track`
   - **Окно**: **Свернутое**

3. Вкладка **Совместимость** (опционально):
   - ✅ Отметить "Запускать эту программу от имени администратора" (если нужно)

4. Нажмите **ОК**

---

### Шаг 3: Измените иконку (опционально)

1. **Правой кнопкой мыши** на ярлыке → **Свойства**

2. Нажмите **Сменить значок...**

3. Выберите иконку или найдите файл с иконкой

4. Нажмите **ОК**

---

## ✅ Готово!

Теперь просто **двойной клик** по ярлыку на рабочем столе:
- ✅ Запустит сервер (без окон)
- ✅ Откроет программу в браузере
- ✅ Всё работает в фоне

---

## 🛠️ Если ярлык не работает

### Проблема: "Не удается найти скрипт"

**Решение:**
- Убедитесь, что путь к `launch_server_silent.vbs` правильный
- Проверьте, что файл существует в папке проекта

### Проблема: "Сервер не запускается"

**Решение:**
1. Проверьте, что Python установлен
2. Проверьте, что сетевой путь доступен
3. Запустите `diagnose_network.ps1` для диагностики

### Проблема: "Появляется окно PowerShell"

**Решение:**
- Убедитесь, что используете `launch_server_silent.vbs` (не .bat или .ps1)
- VBScript запускается через `wscript.exe` без окон

---

## 💡 Альтернатива: Использовать Python скрипт

Если VBScript не работает, можно создать Python-скрипт и скомпилировать его в .exe:

1. Используйте `launch.py` (уже есть в проекте)
2. Скомпилируйте в .exe с помощью PyInstaller:
   ```cmd
   pip install pyinstaller
   pyinstaller --onefile --windowed launch.py
   ```
3. Создайте ярлык на полученный .exe файл

Но VBScript проще и не требует компиляции! ✅


