# Пошаговая инструкция - настройка автозапуска

## 🎯 Самый надёжный способ (рекомендуется)

### Шаг 1: Диагностика

1. Откройте **Проводник Windows**
2. Перейдите в: `\\Desktop-1\новая папка\code\debt-track`
3. Найдите файл **`diagnose_network.ps1`**
4. **Правой кнопкой мыши** → **Запустить с PowerShell**
5. Прочитайте результаты - скрипт покажет все проблемы

---

### Шаг 2: Исправление проблем (если есть)

#### Если "Execution policy is Restricted":
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### Если "Cannot access network path":
Используйте метод с сопоставлением диска (см. Шаг 3)

---

### Шаг 3: Настройка (выберите один способ)

#### Способ A: Сопоставление диска (САМЫЙ НАДЁЖНЫЙ)

1. Откройте **Командную строку как администратор**
   - Win + X → "Командная строка (администратор)"
   
2. Выполните команду:
   ```cmd
   net use Z: "\\Desktop-1\новая папка" /persistent:yes
   ```

3. Перейдите в папку проекта:
   ```cmd
   cd Z:\code\debt-track
   ```

4. Запустите финальный скрипт:
   ```powershell
   powershell -ExecutionPolicy Bypass -File setup_autostart_FINAL.ps1
   ```

**Или используйте готовый batch-файл:**
- Найдите `setup_simple_map_drive.bat` в папке проекта
- **Правой кнопкой** → **Запуск от имени администратора**

---

#### Способ B: Прямой запуск из проводника

1. Откройте **Проводник Windows**
2. В адресной строке введите: `\\Desktop-1\новая папка\code\debt-track`
3. Убедитесь, что папка открывается
4. Найдите файл **`setup_autostart_FINAL.ps1`**
5. **Правой кнопкой мыши** → **Запустить с PowerShell**

Если появится запрос на права администратора - нажмите "Да"

---

#### Способ C: Через PowerShell напрямую

1. **Правой кнопкой** на кнопке Пуск → **Windows PowerShell (администратор)**

2. Выполните команды:
   ```powershell
   # Сначала сопоставьте диск (если нужно)
   net use Z: "\\Desktop-1\новая папка" /persistent:yes
   
   # Перейдите в папку проекта
   cd Z:\code\debt-track
   
   # Запустите скрипт
   .\setup_autostart_FINAL.ps1
   ```

---

### Шаг 4: Проверка

1. Откройте **Планировщик заданий**:
   - Win + R → `taskschd.msc` → Enter

2. Найдите задачу **"PharmacyDebtTracker"**

3. **Правой кнопкой мыши** → **Выполнить**

4. Откройте браузер: `http://127.0.0.1:8000`

5. Если страница открывается - **всё работает!** ✅

---

## 🔧 Если ничего не работает

### Вариант 1: Ручная настройка через Планировщик заданий

1. **Сопоставьте сетевой диск:**
   ```cmd
   net use Z: "\\Desktop-1\новая папка" /persistent:yes
   ```

2. **Откройте Планировщик заданий:**
   - Win + R → `taskschd.msc`

3. **Создайте задачу:**
   - Создать простую задачу → Имя: `PharmacyDebtTracker`
   - Триггер: "При входе в систему"
   - Действие: "Запустить программу"
   - Программа: `Z:\code\debt-track\venv\Scripts\pythonw.exe` (или `pythonw.exe`)
   - Аргументы: `manage.py runserver 0.0.0.0:8000`
   - Начать в: `Z:\code\debt-track`
   - Свойства → "Выполнять с наивысшими правами"

---

### Вариант 2: Использовать локальную копию

Если сетевой путь постоянно недоступен:

1. Скопируйте папку проекта на локальный диск (например, `C:\debt-track`)
2. Запустите скрипт настройки оттуда
3. Или настройте синхронизацию файлов

---

## 📋 Чек-лист

Перед настройкой убедитесь:

- [ ] Компьютер Desktop-1 включен и доступен
- [ ] Вы можете открыть папку `\\Desktop-1\новая папка` в проводнике
- [ ] Python установлен (проверьте: `python --version`)
- [ ] Вы запускаете скрипты от имени администратора
- [ ] Execution policy разрешает выполнение скриптов

---

## 💡 Полезные команды для проверки

```powershell
# Проверить сетевой путь
Test-Path "\\Desktop-1\новая папка\code\debt-track"

# Сопоставить диск
net use Z: "\\Desktop-1\новая папка" /persistent:yes

# Проверить Python
python --version
pythonw --version

# Проверить задачу
Get-ScheduledTask -TaskName "PharmacyDebtTracker"

# Удалить задачу (если нужно пересоздать)
Unregister-ScheduledTask -TaskName "PharmacyDebtTracker" -Confirm:$false
```

---

## 🆘 Если нужна помощь

Запустите `diagnose_network.ps1` и пришлите полный вывод - это поможет понять, в чём проблема.




